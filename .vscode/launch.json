{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node-terminal",
      "name": "Launch app",
      "request": "launch",
      "command": "npm run start",
      "cwd": "${workspaceFolder}/services/app"
    }
  ]
}
